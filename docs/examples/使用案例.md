# 案例1:翻译工作流
> 需要复刻的工作流:

![预期生成](imgs/使用案例/image.png)

> AI使用CLI生成的 工作流
![真实效果](imgs/使用案例/image-1.png)


# 案例2:复杂的 企业客服与工单处理工作流

这个流程里包含：

* 开始节点
* 输入预处理
* 意图识别
* 条件分支
* 知识库检索
* 工具调用【http】
* LLM 回复生成
* 低置信度转人工 【http】
* 循环补问
* 工单创建
* 通知
* 结束


## 流程图

### mermaid图
**大致的流程图:**
```mermaid
flowchart TD
    A([Start]) --> B[输入清洗 / 敏感检测]
    B --> C{意图识别}

    %% 咨询类
    C -->|咨询类| D[知识库检索]
    D --> E{检索结果置信度 >= 阈值?}
    E -->|是| F[LLM生成正式回复]
    F --> Z1([End])

    E -->|否| G[补问节点 / 澄清问题]
    G --> H{用户是否补充信息?}
    H -->|是| D
    H -->|否| I[转人工]
    I --> Z2([End])

    %% 订单类
    C -->|订单类| J[调用订单系统 API]
    J --> K{订单诉求类型}
    K -->|退款| L[执行退款规则校验]
    K -->|催单| M[查询物流与履约状态]
    K -->|改址| N[校验是否允许修改地址]
    L --> O[LLM生成订单答复]
    M --> O
    N --> O
    O --> Z3([End])

    %% 故障类
    C -->|故障类| P[提取报错码 / 日志]
    P --> Q[调用监控 / 服务状态工具]
    Q --> R{是否命中已知故障?}
    R -->|是| S[输出修复方案与文档链接]
    S --> Z4([End])
    R -->|否| T[创建技术工单]
    T --> U[通知责任人 / 群机器人]
    U --> Z5([End])

    %% 投诉类
    C -->|投诉类| V[情绪分析 / 风险评级]
    V --> W{是否高风险?}
    W -->|是| X[直接转人工主管]
    X --> Z6([End])
    W -->|否| Y[LLM安抚并收集关键信息]
    Y --> AA[创建投诉工单]
    AA --> Z7([End])
```

## AI使用CLI生成的工作流效果


采用 codex cli 并使用 gpt-5.3-codex 模型

最终效果:

![最终效果](imgs/使用案例/image-2.png)
附带yaml文件
[对应yaml文件](./enterprise_customer_service_ticket_workflow_http_only.yaml)


# 案例3:售后咨询分流 Chatflow

这个案例是一个适合电商售后场景的 `chatflow`，用于把用户问题按售后类型分流到不同回复分支。它没有接外部系统，重点演示：

* chatflow 模式的基础搭建
* question-classifier 分类分流
* 多个 LLM 专员节点
* 多个 Answer 节点分别输出结果
* 面向多轮对话的 memory 配置

## 适用问题类型

* 退款退货
* 物流催件
* 质量问题
* 复杂问题转人工

## 流程图

```mermaid
flowchart TD
    A([用户售后问题]) --> B{售后问题分类}
    B -->|退款退货| C[退款退货专员]
    C --> D([退款退货答复])
    B -->|物流催件| E[物流催件专员]
    E --> F([物流催件答复])
    B -->|质量问题| G[质量问题专员]
    G --> H([质量问题答复])
    B -->|人工处理| I[复杂售后转人工]
    I --> J([复杂售后答复])
```

## 设计说明

这个 chatflow 的输入只有一个 `query`，由开始节点收集用户售后问题。随后进入 `question-classifier` 节点，根据用户核心诉求把问题分为四类。

每个分类都会进入一个独立的 LLM 节点，提示词分别针对退款退货、物流催件、质量问题和复杂售后升级做了约束，避免所有问题都落到一个通用客服提示词里。每条分支的结果再由对应的 Answer 节点输出。

为了更贴近真实客服对话，这几个 LLM 节点都启用了基础 memory 窗口，适合在 Dify 中继续多轮追问，例如让用户补充订单号、签收状态、物流节点或问题照片。

## 本地验证命令

```powershell
uv run dify-workflow validate docs/examples/after_sales_chatflow.yaml
uv run dify-workflow checklist docs/examples/after_sales_chatflow.yaml
uv run dify-workflow inspect docs/examples/after_sales_chatflow.yaml -m
```

## 示例提问

* 我这单昨天申请了退款，现在还能拦截发货吗？
* 物流三天没更新了，帮我看看是不是丢件了。
* 收到的商品外壳破损，而且少了一个配件。
* 这个订单涉及保修和补偿，我想转人工处理。

![最终效果](imgs/使用案例/image-3.png)
附带yaml文件
[对应yaml文件](./after_sales_chatflow.yaml)
