These workflow fixtures are vendored copies kept inside the repository so the
integration tests do not depend on an external dify-test checkout.

They represent Dify-compatible reference DSL samples used for parser and
validator compatibility checks.